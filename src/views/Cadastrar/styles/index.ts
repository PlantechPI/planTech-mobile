import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    img: {
      marginTop: 120,
      height: 160,
      width: '100%',
      resizeMode:"contain"
    },
  });

  export default styles